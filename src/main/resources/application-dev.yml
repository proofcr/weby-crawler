crawler:
  cron:
    expression: '0 */15 * * * *'
  articleIdForThumbQueueSize: 10
  thumbResizeLoopMillisecondDelay: 500
  headLinesBySitePoolSize: 1

spring:

  profiles:
      include: mysql

  logging:
    level:
      com.crevainera.weby: DEBUG